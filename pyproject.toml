[project]
name = "data-pipeline"
version = "0.1.0"

dependencies = [
    "pandas",
    "pytest",
    "sqlalchemy"
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"